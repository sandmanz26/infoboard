// Shared paging config for the "airport board" rotations, so App.jsx can
// compute how long a level's content takes to complete one lap without
// duplicating the numbers each board's usePagedRows call already uses.
export const BOOKING_PAGE_SIZE = 10
export const BOOKING_PAGE_INTERVAL_MS = 6000
